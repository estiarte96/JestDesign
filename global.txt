:root{
  --fg:#1a1a1a;
  --muted:#666;
  --line:#e5e5e5;
  --bg:#fff;
  --primary:#1b6e3a;

  --font-body:"Montserrat", system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  --font-title:"Oswald", system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  
}

*{ box-sizing:border-box; }

body{
  margin:0;
  font-family:var(--font-body);
  font-weight:600;
  letter-spacing:.1px;
  color:var(--fg);
  background:var(--bg);
  line-height:1.6;
}

a{ color:inherit; text-decoration:none; }

/* Títulos (igual en todas) */
h1,h2,h3,h4,h5,h6,
.h1,.h2,.price{
  font-family:var(--font-title);
  font-weight:800;
  letter-spacing:.6px;
  text-transform:uppercase;
}

/* Texto muted coherente */
.muted, .lead, p.muted{ color:var(--muted); }
